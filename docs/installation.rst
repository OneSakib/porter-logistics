Installation
============

Install via pip:

.. code-block:: bash

   pip install nimbuspost

Or install from source:

.. code-block:: bash

   git clone https://github.com/onesakib/nimbuspost.git
   cd nimbuspost
   pip install -e .

Requirements
-----------

- Python 3.8+
- requests
- Sphinx (for building docs)
